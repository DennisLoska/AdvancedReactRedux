import React from 'react';

export default () => (
  <div>
    <form>
      <fieldset>
        <label htmlFor="email">Email</label>
      </fieldset>
      <fieldset>
        <label htmlFor="password">Password</label>
      </fieldset>
    </form>
  </div>
);
